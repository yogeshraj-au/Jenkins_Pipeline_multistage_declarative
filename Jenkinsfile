pipeline{
agent any

stages {
    stage('Build') {
        steps {
            sh 'echo the Build is passed'
        }
    }

    stage('Unit Test') {
        steps {
            sh 'echo the Test is passed'
        }
    }

    stage('Integration Test') {
        steps {
            sh 'echo the Integration Test is passed'
        }
    }
}
}
